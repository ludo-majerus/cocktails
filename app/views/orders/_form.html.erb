<%= form_for(order) do |f| %>
    <%= f.hidden_field :requester, :value => session[:requester] %>
    <%= f.hidden_field :cocktail_id, :value => cocktail_id %>
    <%= f.submit 'J\'en veux un', :class => 'btn btn-primary' %>
<% end %>
