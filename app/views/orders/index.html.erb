<p id="notice"><%= notice %></p>

<h1>La file d'attente !</h1>

<% @orders.each do |order| %>
	<div class="attente <% if order.done == 1 %>text-right<% end %>">
		<% if order.done == 1 %><s><% end %>
			<%= order.requester %> a commandé un <%= order.cocktail.name %> il y a <%= time_ago_in_words(order.created_at) %>. 
		<% if order.done == 1 %></s><% end %>
		<% if @is_root %>
			<%= link_to 'Fait !', '/orders?done=' + order.id.to_s %>
		<% end %>
	</div>
<% end %>

<br>

<% if @is_root %>
  <%= link_to 'New Order', new_order_path %>
<% end %>

<a style="color:transparent" target="_blank" href="mailto:hellodelu@gmail.com?subject=Je veux un cocktail personnalise !&body=J'ai trouve le lien secret, alors voici le cocktail que je veux maintenant : %0D%0A %0D%0A (ecrire ici le contenu du cocktail, et j'essaierai de le faire, promis !)">Cliquez moi dessus.</a>
